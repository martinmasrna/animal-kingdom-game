# Engine — Backlog

Open items only. Top-3 summary in [`../STATUS.md`](../STATUS.md). M0–M6 shipped; suite 254 passing.

- [ ] **★ Scheduler: per-unit timers gated on top-of-stack (implements the 2026-07-15 timed-effect ruling).** Rules text: [`../rules/overview.md`](../rules/overview.md) §9.1; evidence + blast radius: [`../rules/timed-effect-ruling.md`](../rules/timed-effect-ruling.md). Today `schedule(state, owner, delay, step)` books an **absolute fire-turn** and mostly forgets the unit — Chipmunk (`effects.py:1473`) / Black Bear (`1551`) / Chinchilla (`1591`) carry **no `iid` at all** and pay out even when the unit was destroyed; Grizzly Bear (`1556`) carries one but `_find_unit` (`263`) **scans the whole stack**, so it fires while buried. The Egg/Fig Tree/Watering Hole guards look right only because those cards are Fragile (covering *removes* them). Needed: **turns-remaining per unit**, decremented at the start of the owner's turn only if that unit is currently top of its crossroad; removal cancels; bounce-to-hand clears the timer. `_find_unit` answers "exists anywhere" where the question is "is on top" — callers need a top-of-stack check. Tests per tier: destroyed → cancelled; buried → suspended; uncovered → resumes; bounced+replayed → fresh timer; Fragile+covered → cancelled (unchanged). ⚠ **Gates the baseline-deck arc**: Black Bear + Grizzly Bear are in the ruler's `DECKLIST`, so the ruler must be re-baselined after this lands and before it locks.
- [ ] **Perft-style move-gen regression fixture (from the chess-engine mining).** Chess engines validate move-gen with *perft*: recursively enumerate all reachable positions to a fixed depth, count them, diff against a trusted reference. We have no second engine to diff, but snapshotting legal-action counts over a handful of fixed positions/openings as golden fixtures would catch silent rules/move-gen regressions cheaply (complements the 260-test suite). Rationale in [`../bots/chess-engine-techniques.md`](../bots/chess-engine-techniques.md).
- [ ] **State-representation speed (before NN bots).** Engine uses per-unit `UnitInstance` objects — fine for thousands of greedy games, *not* for MCTS/AlphaZero (millions of cheap clones + batched feature extraction). When there: switch the in-play representation to struct-of-arrays / entity-component (parallel int arrays, array-copy clones) + a tensor per-seat encoding. Localized to `state.py` (the data layer and id-keyed effect registry are already decoupled). Measure clone cost first; keep `UnitInstance` lean (`__slots__`) meanwhile. *Parked until NN bots.*
