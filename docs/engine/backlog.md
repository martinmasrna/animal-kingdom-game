# Engine — Backlog

Open items only. Top-3 summary in [`../STATUS.md`](../STATUS.md). M0–M6 shipped; suite 254 passing.

- [x] **Scheduler: per-unit timers gated on top-of-stack — SHIPPED 2026-07-15.** Implements the timed-effect ruling; details in [`../rules/timed-effect-ruling.md`](../rules/timed-effect-ruling.md). `schedule()` takes the unit and stores `remaining`; `start_of_turn()` ticks only for top-of-stack units, cancels entries whose unit left the board, fires at zero. Bounce-reset fell out for free (replay mints a new iid). Knock-on caught: `GreedyWeights.pending_payoff` discounted by a *global*-turn horizon, so the bot now reconstructs it as `2*remaining - (0 if current == owner else 1)` — identical numbers, weight keeps its validation. **Remaining → Balance: re-baseline the ruler** (Black Bear + Grizzly Bear are in `benchmark_set.DECKLIST`).
- [ ] **Landmark type machinery is now dormant — decide whether to remove it (→ Code Health).** The 2026-07-15 animals-only cut left `LANDMARK_IDS` empty, so `Card.is_unit` is always True and **7 `is_unit` branches in `effects.py` are dead conditions** (HQ capture `108`/`479`, unit-filtered draws `672`, targeting `845`/`1210`/`1269`/`1376`), plus the `type` field, the decision-C validation (`cards.py:112`), and `draw_filtered_random`'s now-unused `strength_min` spec (Watering Hole was its only caller). Deliberately left in place with the cards' removal (Vulture precedent) rather than ripped out in the same change. Removing it touches the HQ-capture and targeting paths, so it wants its own diff and its own test run.
- [ ] **Perft-style move-gen regression fixture (from the chess-engine mining).** Chess engines validate move-gen with *perft*: recursively enumerate all reachable positions to a fixed depth, count them, diff against a trusted reference. We have no second engine to diff, but snapshotting legal-action counts over a handful of fixed positions/openings as golden fixtures would catch silent rules/move-gen regressions cheaply (complements the 260-test suite). Rationale in [`../bots/chess-engine-techniques.md`](../bots/chess-engine-techniques.md).
- [ ] **State-representation speed (before NN bots).** Engine uses per-unit `UnitInstance` objects — fine for thousands of greedy games, *not* for MCTS/AlphaZero (millions of cheap clones + batched feature extraction). When there: switch the in-play representation to struct-of-arrays / entity-component (parallel int arrays, array-copy clones) + a tensor per-seat encoding. Localized to `state.py` (the data layer and id-keyed effect registry are already decoupled). Measure clone cost first; keep `UnitInstance` lean (`__slots__`) meanwhile. *Parked until NN bots.*
