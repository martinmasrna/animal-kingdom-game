# Balance — Backlog

Open items only. Top-3 summary in [`../STATUS.md`](../STATUS.md). Two targets: every **deck** in **40–60%** winrate; every **card impact** within **±10%**. Roadmap: `simulation-platform-roadmap.md`. All conclusions **gated on trustworthy pilots (→ Bots)**.

- [ ] **Apply and re-test the locked card review:** [`card-balance-todo.md`](card-balance-todo.md)
  records the Cat trims, Aggro/Egg rarity moves, new Aggro designs, reviewed non-changes, and the
  deliberately short post-change watch list.
- [ ] **Deck equality → pull every deck into 40–60%.** Current Greedy-Bot outliers from the
  200-game full matrix: cats_midrange ~75% at the top; colony_food_swarm ~30%, food_otk ~34%,
  and egg_control ~38% at the bottom. Colony's lack of a favorable matchup is the strongest weak-deck
  signal; Food OTK is especially pilot-sensitive and should not be tuned from Greedy play alone.
  - **Decision H — re-derive the food economy on one shared scale** (vs `win_food` 100, region 10/20). *In progress:* Methuselah 10→5 and the food_otk one-off floor (Chipmunk 10+10, Squirrel 12, Flying Squirrel 8, Gazelle 40) shipped. **Still open:** the 20-cost bodies (Elephant / Borealis / Aquila / Bulwark) + Fig Tree / Watering Hole Landmarks; Egg Eater 10 / Vulture 5 / Eon 1-per-event; Colony numbers (Marabunta / Honoria / Worker Ant / Worker Bee / Falstaff); Scrooge doubler. Keep every number a placeholder in `engine/config.py`.
  - ⚠ **Watch: Carmilla OTK too consistent/early? — RESOLVED, de-escalate (2026-07-03).** The feared one-turn ≥`win_food` kill (Carmilla sac'ing 3 Gazelles = 120 in one Battlecry) **essentially never assembles in play: ≤0.5% of games** at the settled 2-action ruleset (instrumented max single-turn food gain, food_otk vs the 7-deck pool, greedy/turn/referee pilots — `scratchpad/instrument_otk.py`). Getting Carmilla + all 3 Gazelles onboard-and-adjacent in one turn is far too specific. The deck's *actual* payoff is a **repeatable single-Gazelle ~40 swing** (a ≥40 burst fires in 52–64% of search-piloted games) plus the standalone gainers — a food *race*, not a one-shot 120 combo. So Carmilla/Gazelle are not over-consistent; leave them. (Still a Decision-H dial like any other, just not a live OTK-safety concern.)
  - **✅ VERDICT — food_otk OTK-realism review: the flagged phenomenon was a STALE-RULESET ARTIFACT, not a real signal (2026-07-03).** Split resolved as **neither (a) nor (b): (c) the acceptance artifact was run on the wrong ruleset.** The `results/bot_quality/turnbot/` cohort (and the referee triage) ran at **map_b + 1-action** (`meta.config: null` — the `bot_comparison` default is `map_id=map_b` but `--config` unset ⇒ `actions_per_turn=1`), which is **not the settled ruleset (map_b + 2-action)**. With only one action/turn you cannot place fodder *and* detonate a sac in the same turn, so the combo physically can't come together → myopic banking ≈ optimal → the deck *looks* like a goldfish where greedy beats search. That is an artifact of the mismatched ruleset, **not** a property of the deck (a) or the evaluator (b).
    - **Re-ran the exact acceptance benchmark at the settled 2-action ruleset (only `config` changed):** paired, both seats, 200 games/opp × 7, map_b, seed 683470156, `--config two_action_config.json` → **food_otk turn +15.6% [+12.6%, +18.7%] over greedy** (greedy 34.4% → turn 50.1%, mirror 67%, all strength gates pass; artifact `results/bot_quality/food_otk_2action_turn/`). The sign **flips** vs the artifact's −9.4% [−12.2, −6.6] and the CIs don't touch. RefereeBot directional read (105 games, 2-action) is likewise **57%**, well above greedy's ~38% — the *whole* search ladder reverses. Mechanistically coherent: **greedy scores ~33% either ruleset** (it can't combo regardless), while **turn goes 23%→50%** exactly when handed the 2nd action needed to assemble+fire the sac loop. **Corroborated by the full 7-deck 2-action re-run** (`results/bot_quality/turnbot_2action/`, 100/opp): food_otk turn **+16.4%** [+12.3,+20.7], and *every* deck improves under turn — the 1-action cohort was systematically misleading, not just for food_otk.
    - **The OTK identity is REAL under the settled ruleset.** Search fires a ≥40 Gazelle burst in 52–64% of games and wins ~64–78% of those; the deck is a search-rewarding sac-combo, not a goldfish. **No card change and no evaluator change is warranted** — this is not a nerf-worthy (a) nor a bot-fix (b).
    - **Trust of food_otk balance numbers:** the `results/bot_quality/turnbot/` food_otk numbers are **invalid (wrong ruleset)** — discard. At the settled ruleset, **greedy pilots food_otk badly (GREEDY_CAVEAT — no combo), so greedy-based food_otk winrates stay untrustworthy; use turn/referee.** A real food_otk *deck-balance* verdict (is it in 40–60%?) still needs the full 2-action matchup matrix with a **search pilot on both sides** — that's the next Balance step, separate from this OTK-realism review. The prior "food_otk overrated" note also traces to greedy/1-action reads and should not be trusted as-is.
    - **→ Handback to Bots + Meta:** the acceptance cohort ran map_b + 1-action for **all 7 decks** — food_otk's sign flips; others may shift too. Re-run the acceptance cohort at 2-action before the `./report`-default decision, and fix the `bot_comparison` footgun (default `map_b` silently paired with 1-action `config=None`). See `../bots/backlog.md`.
  - **Card-design fixes for the confirmed-weak decks:** colony_food_swarm needs a cheap efficient early blocker or a faster engine payoff (loses to cats/canine before it turns on). ~~food_otk protection/kill-window~~ **struck** — that need was inferred from the stale 1-action reads; at the settled 2-action ruleset a search pilot executes the deck to 50%+, so **do not** touch food_otk cards until a search-vs-search 2-action deck-balance read says so (see the ✅ verdict above).
- [ ] **Card equality → every card impact within ±10%.** Blocked on the `metrics.py impact` fix (→ Engine) and trustworthy pilots (→ Bots).
- [ ] **Watch: cats_midrange vs ramp.** Moved from ramp-favored to even after the keyword split (Snow Leopard now extends to Tiger). "Even" is acceptable; further drift toward cats is not.
- [ ] **Verify still relevant (legacy "Combo" refs).** Queen Bee — keep additive (+F per food gain), watch multi-copy stacking. Hibernating Bear — confirm the 2-turn delay + "lose all food" + Immovable can't be abused. (Both name the retired "Combo" deck; confirm against the current pool.)
