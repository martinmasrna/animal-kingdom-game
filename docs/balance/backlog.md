# Balance — Backlog

Open items only. Top-3 summary in [`../STATUS.md`](../STATUS.md). Two targets: every **deck** in **40–60%** winrate; every **card impact** within **±10%**. Roadmap: `simulation-platform-roadmap.md`. All conclusions **gated on trustworthy pilots (→ Bots)**.

- [x] **Applied 2026-07-04 — the locked card review:** [`card-balance-todo.md`](card-balance-todo.md)
  (Cat trims, Aggro/Egg rarity moves incl. Stoop↔Gale swap, Black Swan cap, Goliath demotion,
  Vulture shelved). Suite green (308/1xfail). **Not yet re-tested** — bundled into the same
  ~2h validation run as the food_otk/egg_control Decision H buffs below (small-change sims
  don't justify a standalone run per this session's constraint). Watch list still open: see
  `card-balance-todo.md`'s "Quick follow-up sweep."
- [ ] **★ Fixed-pilot re-baseline (2026-07-05) — supersedes the oracle-corrected table below.** After shipping `GreedyWeights.pending_payoff=20` (the scaling/delayed-effect eval fix, commit ad4c885; turn/referee share the evaluator so they inherit it), regenerated the 7×7 matrix with BOTH strong pilots directly — no oracle-correction needed. Provenance: `results/matrix_referee_pending20/` (referee,referee, 150 games/matchup) + `results/matrix_turn_pending20/` (turn,turn, 200), seed 0, map_b + 2-action, both seats. Deck strength (both-seat avg); the two pilots agree tightly (spread ≤4.9pt) → deck-level numbers reliable:

  | deck | referee | turn | avg | prev ~corrected | verdict |
  |---|---:|---:|---:|---:|---|
  | cats_midrange | 62.6 | 63.0 | **62.8** | ~69 | too strong — but LESS than before |
  | ramp | 60.0 | 58.5 | 59.2 | ~61 | borderline-high, watch |
  | aggro_hq_rush | 60.2 | 56.0 | 58.1 | ~60 | borderline-high, watch |
  | colony_food_swarm | 45.1 | 46.8 | 46.0 | ~48 | ✓ in band |
  | egg_control | 44.5 | 42.4 | **43.5** | ~36 | ✓ **NOW IN BAND** |
  | canine_buff_tempo | 42.3 | 43.1 | 42.7 | ~51 | ✓ in band |
  | food_otk | 35.3 | 40.2 | 37.7 | ~25 (pre-buff) | still slightly weak |

  - **✅ egg_control RESOLVED — close the buff question.** Its old ~36% *was* the pilot-under-values-scaling artifact the notes diagnosed; the pending_payoff fix confirms it — egg is 43.5%, in band, **no card change** (validates both the diagnosis and the fix). See [[egg-scaling-blindspot-confirmed]].
  - **cats recalibrated down (~69 → ~63).** Still **#1 nerf target**, and the nerf **magnitude should be smaller** than the old 69% implied. ⚠️ **But the prior "cats may be inflated because the pilot under-plays scaling" caveat is only *partly* resolved.** cats deflated mostly because **ramp** (a delayed-payoff deck the fix lifted) now beats it (cats-vs-ramp ~29%) — **not** because egg's scaling got piloted: **egg-vs-cats is still ~24% (bot) vs ~50% (human)** (the multi-turn scaling plan is still un-piloted — see `../bots/backlog.md` dynamic-strength item). So ~63% may *still* be mildly inflated; **confirm the cats-nerf magnitude against human play into cats** before committing.
  - **food_otk still ~38%** after the 2026-07-04 OTK-lean buffs — near the 40% floor but not over it; the pending Pufferfish/common-slot follow-up (below) may close it. Highest pilot spread (4.9pt) → lowest-confidence cell.
  - ramp/aggro ~58–59% borderline-high — watch, don't act.

- [ ] **Deck equality → pull every deck into 40–60%.** **Current best read = the turn-vs-turn 7×7 matrix at the settled ruleset, oracle-bias-corrected (2026-07-03).** Both-seats competent piloting (`results/matrix_turn/` — regenerate via `./report 200 --seed 0 --bots turn,turn --format files --out results/matrix_turn`; greedy baseline in `results/matrix_greedy/`), then adjusted by each deck's referee-underplay gap (`results/bot_quality/turn_vs_referee_all/`, see `../bots/backlog.md` pilot-trust item). Corrected standings:

  | deck | greedy WR | turn WR | oracle-corrected* | verdict |
  |---|---:|---:|---:|---|
  | cats_midrange | 74.6% | 72.1% | **~69%** | too strong (clear) |
  | ramp | 58.2% | 54.4% | ~61% | mildly high |
  | aggro_hq_rush | 60.2% | 59.6% | ~60% | borderline high |
  | canine_buff_tempo | 54.8% | 47.8% | ~51% | ✓ in band |
  | colony_food_swarm | 30.4% | 51.9% | ~48% | ✓ in band |
  | egg_control | 38.0% | 34.2% | ~36% | too weak |
  | food_otk | 33.7% | 30.1% | **~25%** | too weak (clear) |

  *rough: turn WR ± (avg-field underplay − this deck's referee gap); directional, not precise. Spread barely compresses greedy→turn→corrected (~44→42→~44 pt), so **the imbalance is real, not a pilot artifact.**
  - **✅ RETRACTED — colony_food_swarm is NOT a weak deck (2026-07-03).** Its greedy ~23–30% (long the project's "strongest weak-deck signal") was a **piloting artifact**: greedy can't run the swarm/queen engine. Under competent both-sides play it jumps **+21.5pt to ~52% (raw) / ~48% (corrected) — in band.** Drop it from the confirmed-weak list; leave its cards alone. (Its lack of a *favorable* matchup was the old signal — that too dissolves under competent play.)
  - **Real, pilot-robust signals to act on:** **cats_midrange too strong (~69%)** — #1 nerf target, robust across greedy/turn/referee + both maps (Prince Leo/Princess Lea twin-summon is the engine). **food_otk (~25%) and egg_control (~36%) too weak** — the two buff targets. Read per-card `impact` as a within-deck, same-rarity relative signal. ramp/aggro borderline-high — watch, don't act yet.
  - **✅ egg_control — leave the cards alone; its low sim number is a pilot artifact (2026-07-04).** Human playtest (egg vs referee-cats, 12 games, `results/human_games/ad-hoc/`) went ~50% vs the bot matrix's ~18% for the same matchup. egg out-scales cats' midrange by design (Rattlesnake→8 off shuffles, Goliath→11 off the remove pile, Anaconda apex 7 vs cats topping at King Theron 8); the search can't value that late-game scaling, so it never executes the plan. → Bots item (search under-values dynamic strength, see `../bots/backlog.md`), **not** an egg card change. **Matrix caveat:** if the pilot under-plays scaling/HQ-capture win conditions, the turn/greedy matrix likely under-rates decks that use them and over-rates pure board/food-control decks — so **cats' ~69% may be partly inflated**; hold the cats-nerf magnitude until a human plays other decks into referee-cats.
  - **✅ RETRACTED (2026-07-04) — "buff the value plan, not the OTK" was the wrong read.** The original note here said food_otk's combo enablers (Scrooge/Impala/Pufferfish) carrying the worst per-card impact meant leaning away from the OTK. Corrected: `impact` measures how often the *whole plan* lands around a given draw, not that card's power in isolation — Scrooge being worst-impact means the deck doesn't yet protect/deliver its 2-turn window, not that the combo is bad. The OTK is the deck's identity (confirmed real, fires 52–64% of games — see the ✅ verdict below); the fix leans **into** it. See `../../HANDOFF.md` for the full correction.
  - **food_otk OTK-lean buff pass — applied 2026-07-04, pending sim validation.** Diagnostic target: Scrooge flips from worst-impact to best-impact card *without changing Scrooge's own fields* (`scrooge_delay`/`scrooge_multiplier` untouched) — the fix has to come from the rest of the deck surviving to cash it in. Changes (all `food_otk`-exclusive cards, clean blast radius, no other deck touched): **Opossum** Battlecry now also gains `opossum_food` (new dial, 5) — recyclable via Carmilla/Black Widow sac loops, so it compounds into what Scrooge later doubles. **Giant Tortoise** and **Porcupine** str 5→7 — answers "the deck's max str is 5, can't contest anything 5+"; Tortoise (Immovable) becomes buriable only by str 8+ and never removable, Porcupine (already "quills beat teeth" vs Apex per `keywords.md`) becomes un-buriable full stop. **Pufferfish** now also draws 1 card when its trap fires — it so rarely triggers against a competent opponent that the payoff needed raising, not the trigger rate. **Gazelle** `gazelle_food` 40→30 — the single-hit swing overshadowed Impala's draw-2 payoff; budget moved into the four buffs above. **Watch:** Impala is the fallback lever if it's still the worst-or-tied-worst card after this pass. Suite green (309/1xfail).
  - [ ] **TODO (not yet done) — bench Pufferfish from food_otk + fill the freed common slot.** Pufferfish stays at −4.3% impact even *with* the 2026-07-04 draw-1 buff (the trap just never fires vs a competent pilot), and it's the deck's only Fish — off-theme for the sac/hoard engine. **Plan:** shelve it to `card-candidates.md` (like Vulture), leaving one **common ×3** slot open (deck stays 4-4-6). Fill it to serve the OTK plan — the diagnosed bottleneck is *combo-assembly reliability* (Scrooge's pile is too small when it lands), so the lean is a **third cheap sac outlet that also pays food** (converts Deathrattle fodder → food+cards faster, feeding Scrooge's double); alternatives floated were a recurring Immovable "hoarder" body and more Deathrattle burst fodder. **Blocked on:** user's play-feel call on which direction fits. Not started — Pufferfish is still in the deck as of this note.
  - **Decision H — re-derive the food economy on one shared scale** (vs `win_food` 100, region 10/20). *In progress:* Methuselah 10→5 and the food_otk one-off floor (Chipmunk 10+10, Squirrel 12, Flying Squirrel 8, Gazelle 40→30, see the OTK-lean pass above) shipped. **⚠ Corrected 2026-07-15 — they are 15-cost, not 20.** `a27f0df` cut Elephant/Borealis/Aquila/Bulwark 20→15 in `cards.json` and every doc kept saying 20; a dead `config.costs_20_food = 20` (unread by any code) made it look tunable from config. It is **card-intrinsic** (`food_cost` in `cards.json`, gated at `effects.py:462`) — tune it there. Constant deleted, text↔`food_cost` guard added (`test_card_text_consistency.py`). **Ramp's real math:** 6 costed copies × 15 = **90 food vs a 100 `win_food`** — the deck can deploy its payoffs *or* threaten the food win, not both (at 20 it was 120, unaffordable in full). That tension is the substance of this item for ramp. **Still open:** the 15-cost bodies (Elephant / Borealis / Aquila / Bulwark) + Fig Tree / Watering Hole Landmarks; Egg Eater 10 / Vulture 5 (now moot, shelved) / Eon 1-per-event; Colony numbers (Marabunta / Honoria / Worker Ant / Worker Bee / Falstaff). **Scrooge doubler removed from this list** — explicitly not a lever, see the OTK-lean pass above. Keep every number a placeholder in `engine/config.py`.
- [ ] **Card equality → every card impact within ±10%.** Blocked on the `metrics.py impact` fix (→ Engine) and trustworthy pilots (→ Bots).
- [ ] **Watch: cats_midrange vs ramp.** Moved from ramp-favored to even after the keyword split (Snow Leopard now extends to Tiger). "Even" is acceptable; further drift toward cats is not.
- [ ] **Verify still relevant (legacy "Combo" refs).** Queen Bee — keep additive (+F per food gain), watch multi-copy stacking. Hibernating Bear — confirm the 2-turn delay + "lose all food" + Immovable can't be abused. (Both name the retired "Combo" deck; confirm against the current pool.)
