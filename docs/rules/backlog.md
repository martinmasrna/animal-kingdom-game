# Rules & Mechanics — Backlog

Open items only. Top-3 summary in [`../STATUS.md`](../STATUS.md). Canonical rules: `overview.md`; keyword registry: `keywords.md`.

- [ ] **★ Implement the timed-effect ruling (RULED 2026-07-15, engine does not match).** Canonical text: [`overview.md`](overview.md) §9.1; evidence + blast radius: [`timed-effect-ruling.md`](timed-effect-ruling.md). A pending timer advances **only while its unit is top of its crossroad** (buried = suspended, not lost); **removal cancels**; **bounce resets**. Today the scheduler books an absolute fire-turn and mostly forgets the unit: **Chipmunk / Black Bear / Chinchilla pay out even when destroyed** (no `iid` at all) and **Grizzly Bear strikes from under a stack**; the Egg/Fig Tree/Watering Hole guards are right only *by accident* (they're Fragile, so covering removes them). Needs a scheduler change (per-unit turns-remaining gated on top-of-stack, not a booked fire-turn) — → Engine. ⚠ **Black Bear + Grizzly Bear are inside the baseline ruler** (`sim/benchmark_set.py`), so this must land **before the ruler locks** or every card price in the project inherits the bug (→ Balance, the baseline-deck arc). It is a *nerf* to all four cards, two of them food_otk's (already weakest ~38%) — fix the rule, then re-balance; don't bend it to protect a number.
- [ ] **Review expansion mechanics:** [`expansion-mechanics-todo.md`](expansion-mechanics-todo.md)
  specifies proposed Burrow, Camouflage, Hatch/Incubate, movement, tokens, and Venom; it also
  identifies the existing Flight-versus-HQ wording conflict that must be settled before more reach
  cards are approved.
- [x] **Landmark card-type decision — RESOLVED 2026-07-15: CUT.** Was provisional (`../cards/decks/README.md` decision C): a non-animal `Landmark` type (Fig Tree, Watering Hole). **Decision: remove both cards and commit to "every card is an animal, or something that becomes one."** This dissolves the whole item — no type to confirm, and the Apex-eats-Landmark flavor wart goes with it.
  **⚠ Correction (2026-07-15):** an earlier note here claimed the Landmark type "was never implemented, so the cut is 2 cards, not a type migration." **That was wrong** — it came from grepping a `card_type` field that doesn't exist; the field is `type`, and it is very much implemented: `type: "landmark"` on both cards, `Card.is_unit` returns False for them, `cards.py:112` validates that only `LANDMARK_IDS` may be landmarks (decision C), and `is_unit` gates **7 behaviours** in `effects.py` (HQ capture at `108`/`479`, unit-filtered draws at `672`, and targeting at `845`/`1210`/`1269`/`1376`). Cutting the two cards makes `is_unit` **always True**, so the whole type concept goes vestigial — that *is* a type migration.
  **Handling (follows the Vulture precedent):** cut the 2 cards; leave the type machinery dormant rather than ripping it out in the same change. Removing `type`/`is_unit`/`LANDMARK_IDS` and the 7 dead conditionals is a separate cleanup → Code Health. **Eggs are NOT affected** — they're "something that becomes an animal" per `../cards/cards.md` §2, they're real in the data (tagged; Egg Eater + hatch reference them), and cutting them would delete egg_control's identity. Consequences → Cards: ramp needs 2 replacement commons, and ~20 candidate entries lapse (14 Landmark candidates + 6 cards that reference Landmarks). Execution is gated on the in-flight benchmark run (`cards.json` must not change mid-run).
- [ ] **Re-examine Immovable (naming + effect footprint).** Ruled & shipped 2026-07-02 (physics split — see `keyword-review-immovable-untargetable.md`) but still "feels a bit off." *Gated on referee-quality balance data (→ Balance).* Questions: is blocks-own-sacrifices the right cost? Is "Immovable" the right name (it also blocks *destruction*, not just movement)? Candidates: rename (Rooted/Colossal) or split move-immunity from remove-immunity. Walk every card that has / cares about / interacts with Immovable + Stealth as a pair.
- [x] **Terminology sweep: "Discard" → "Remove Pile." DONE 2026-07-15.** One shared Remove Pile (no separate discard pile). Two trigger tiers: *remove* (any card sent there) vs *Deathrattle* (a unit leaving the board). `overview.md` §3.7 renamed 2026-07-08; **2026-07-15:** `cards.json` verified clean (zero occurrences — the shipped convention is Goliath's "removed units" / Black Swan's "removes a random card from their hand"); swept `card-candidates.md` (Hamster), the deck docs (Rat cost, hellhound recursion, Black Swan), and the `decks/README.md` reconciliation TODO; fixed two verb slips in `keywords.md` itself ("discarded normally" → "removed normally"; "a discard" → "Black Swan's hand-remove").
  **Deliberately NOT swept — `cards.md`.** Its four refs (Anaconda "=discard", Jackal, Raccoon, the vulture flavor line) describe **cards that no longer exist as written**: shipped Anaconda is STR 7 "Apex Predator." (Goliath is the removed-units scaler), shipped Jackal is "adjacent unit removed → +5 food", and Raccoon isn't in the pool at all. Renaming there would yield correctly-worded descriptions of cards that aren't in the game. → the file needs a staleness verdict (supersede / rewrite / delete), not a terminology pass: see [`../cards/backlog.md`](../cards/backlog.md).
  **Left as correct English, not the zone:** `overview.md` naming the retired term to retire it; `mental-model.md` "buried, not discarded"; the bots docs' "discards per-game outcomes"; `aggro-redesign.md`'s "discard/self-mill theme" (a design-theme name); the Immovable review doc (a historical record).
- [ ] **Rename the "Battlecry" keyword** (the "when placed" trigger) to something on-theme. Placeholder borrowed from elsewhere; rejected so far: Instinct, Pounce. Card text keeps printing "Battlecry:" until settled.
- [ ] **Rename the "Deathrattle" keyword** (the "when removed / leaves play" trigger) to something on-theme. In active use (Opossum, Impala, Gazelle, Phoenix). Standardize on printing "Deathrattle:" (card-text sweep lives in `../cards/backlog.md`).
